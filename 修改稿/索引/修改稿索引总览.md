# 修改稿索引总览

最后更新：2026-04-19 16:00 +0800

## 固定目录

- 修改稿根目录：`修改稿`
- 操作日志目录：`修改稿/操作日志`
- 索引目录：`修改稿/索引`

## 系列索引

- `索引/v1修改稿索引.md`
- `索引/v2修改稿索引.md`

## 当前活跃状态

- `v1`：本轮未处理
- `v2`：当前最新交付为 `v2d`
- `v2d` 工作输出目录：
  - `修改稿/v2说明文件/v2d/`
- `v2d` 归档目录：
  - `修改稿/v2修改稿留底/v2d/`
- 本轮新增操作日志：
  - `修改稿/操作日志/20260419_1600__v2__v2d__produce_v2d_revision_bundle.md`

## 当前规则摘要

- 工作稿目录使用 `v1初始版` / `v2初始版`。
- 修改建议目录使用 `v1修改建议/<token>修改建议/` 与 `v2修改建议/<token>修改建议/`。
- 说明文件目录使用 `v1说明文件/<token>/` 与 `v2说明文件/<token>/`。
- 留底目录使用 `v1修改稿留底/<token>/` 与 `v2修改稿留底/<token>/`。
- 新稿完成后需要同步：
  - 说明文件
  - 归档留底
  - 索引
  - 操作日志
  - Git 提交
