# v2e Revision Tasklist

## 已完成

1. 从 v2d 最新 Markdown 和对象保留 Word 底稿生成 v2e 正式正文。
2. 建立 `修改稿/v2说明文件/v2e/` 与 `修改稿/v2修改稿留底/v2e/`。
3. 将 v2e 省年审计数据和 implied city-slot registry 的使用边界写入正文、附录说明、验证报告和交付说明。
4. 在 Word 正式稿中把核心 TWFE 方程、处理变量定义、DID 替代口径和 log-ratio 指标改为 Office Math 公式对象。
5. 维持 `treat_share` 多期 DID/TWFE 为唯一主识别；trend-adjusted DID、逐省剔除、wild cluster bootstrap 等仅写作防守性检验或边界诊断。

## 仍未完成/不能冒充完成

1. 没有真实 city-year treatment registry 原始城市名单；当前 registry 为 implied city-slot 审计资产。
2. 没有处理阈值底表；当前阈值字段只能标记为 unavailable。
3. 独立机制变量仍处于变量设计与数据需求阶段，尚未形成正式机制估计结果。
4. 文本效度审计尚无人工双标注、Cohen's kappa 或 Krippendorff's alpha 实际结果。
5. 14 篇 DID 文献的 PDF 级元数据核验仍待人工或数据库逐篇确认。

---

## 2026-04-20 working proxy 执行更新

- 已按人工确认将 implied city-slot registry 作为 v2e working analysis input。
- 已按人工确认将 province-year audit 作为 working threshold/audit proxy。
- 已生成 working threshold proxy、registry by-year/by-province 分析表、机制变量可得性矩阵、文本效度数据可得性矩阵。
- 已生成项目级公告/披露完整度候选 proxy；它不是正式机制估计。
