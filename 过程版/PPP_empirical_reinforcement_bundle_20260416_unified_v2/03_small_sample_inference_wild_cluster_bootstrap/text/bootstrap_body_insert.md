# Small-sample inference body insert

在更保守的小样本推断框架下，`exec_share` 与 `proc_share` 的方向仍与基准 DID 保持一致，但 bootstrap p 值分别为 0.076 和 0.122，说明推进结构结果在更保守推断下仍具有方向稳定性，但统计强度更为敏感。相比之下，`ppp_quality_zindex` 的 bootstrap p 值为 0.237，继续不支持将质量型口径写成稳定主结论。由此，这一层检验更适合作为边界说明，而不应与正文主识别或趋势调整型防守检验处于同一层级。
